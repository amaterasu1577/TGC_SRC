---
# vars file for role_siebel
siebel:
  user: vagrant
  home: /home/vagrant

privatekey:
  passphrase: 'M6~UJLoRqviY(V)gAxhv'

certificate:
  commonName: "awx"
  subjectAlternativeNames: [ "awx", "awx.local"]
  countryName: "FR"
  passphrase: "{{ privatekey.passphrase }}"
  directory: "{{ siebel.home }}/certs"
